<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.umn.w10_27137">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.W10_27137">
        <service
            android:name=".TutorialIntentService.CustomBoundService"
            android:enabled="true"
            android:exported="true"></service>
        <service
            android:name=".TutorialIntentService.CustomService"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".TutorialIntentService.SimpleIntentService" />

        <activity android:name=".TutorialIntentService.IntentServiceActivity" />
        <activity android:name=".TutorialAsyncTaskLoader.AsyncTaskLoaderActivity" />
        <activity android:name=".TutorialAsyncTask.AsyncTaskActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>